"""App config for cheating app."""

from django.apps import AppConfig


class CheatingConfig(AppConfig):
    """Configuration for the Cheating app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "cheating"
